import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1, 
        marginBottom: -25,
        backgroundColor: "#fff",
    }
})

export default styles