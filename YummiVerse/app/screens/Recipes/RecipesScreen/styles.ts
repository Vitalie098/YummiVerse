import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1, 
        marginBottom: -55,
        backgroundColor: "#fff",
    }
})

export default styles