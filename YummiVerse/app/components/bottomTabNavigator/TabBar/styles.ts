import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 27,
    width: 65
  },
  iconContainer: {
    alignItems: "center" 
  }
})

export default styles